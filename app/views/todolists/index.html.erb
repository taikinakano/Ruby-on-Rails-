<h1>投稿機能一覧</h1>
<% @lists.each do |list| %>
 <p>タイトル</p>
 <span>
  <%= link_to list.title, todolists_path(list.id %>
  </span>
  <p>画像</p>
  <%= attachment_image_tag list,format"jpeg",size:"100x100" %>
 <% end %>